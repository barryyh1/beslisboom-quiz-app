# Beslisboom → Quiz (Vite + React + Tailwind)

## Scripts
- **Install**: `npm ci`
- **Dev**: `npm run dev`
- **Build**: `npm run build`
- **Preview**: `npm run preview`

## GitHub Pages
Set `VITE_BASE_PATH=/<repo-name>/` during build if using Pages under a subpath.
